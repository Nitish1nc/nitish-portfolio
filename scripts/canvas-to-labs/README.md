# Canvas to Labs

Automation spec for porting a Cursor `.canvas.tsx` interactive into a deployable Nitish Labs experiment folder.

**Source of truth:** [PLAYBOOK.md](./PLAYBOOK.md)

Use the playbook as a mechanical checklist for agents or scripts. The reference contract in that file is the `inner-voice/` ship (`Why Some Writing Sounds Like Talking`).

For the generalized **note / schema → page** scaffolder (multi-archetype, deterministic CLI), see [../note-to-dashboard/README.md](../note-to-dashboard/README.md) (`npm run n2d`).
